export type Gif = {
    id: string,
    name: string,
    description: string,
    price: string
}

export type GifWithoutId = {
    id: string,
    name: string,
    description: string,
    price: string
}
